import styled from "styled-components"

const ArticleListDivider = styled.hr`
  width: 100%;
  margin: 16px 0;
  border: 1px solid var(--tertiary);
  color: var(--tertiary);
`
export default ArticleListDivider
